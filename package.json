{
	"scripts": {
		"dev:web": "turbo run dev --filter @july_cm/web",
		"build:web": "turbo run build --filter=web...",
		"build:react-lib": "turbo run build --filter @july_cm/react-lib",
		"dev:markdown-helper": "turbo run dev --filter @july_cm/markdown-helper"
	},
	"devDependencies": {
		"turbo": "^1.6.1"
	}
}
